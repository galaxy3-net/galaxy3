#!/usr/bin/env bash

export CDIR="$(dirname ${0})"
export CDIR="$(sed 's/ /\\ /g' <<<${CDIR})"

echo CDIR ${CDIR}
#$(dirname ${0})/naboo ${*} 2>&1 | tee -a /tmp/naboo.log
${CDIR}/naboo ${*} 2>&1 | tee -a /tmp/naboo.log
