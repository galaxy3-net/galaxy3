#!/usr/bin/env bash

export CDIR="$(dirname ${0})"
export CIDR="$(echo ${CIDR} | sed 's/ /\\ /g')"

echo CDIR ${CDIR}
#$(dirname ${0})/naboo ${*} 2>&1 | tee -a /tmp/naboo.log
"${G3HOME_ESC}/bin/naboo" ${*} 2>&1 | tee -a /tmp/naboo.log
