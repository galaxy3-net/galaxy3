$!/usr/bin/env bash

REPO=${1}
REPO_PATH=${2}
BRANCH="${3}

su - thedr -c bash -c "ls -dl /repos/${REPO} 2>/dev/null || cd /repos ; git clone ${REPO_PATH}"
su - thedr -c bash -c "cd /repos/${REPO} && git pull && git checkout ${BRANCH}"
